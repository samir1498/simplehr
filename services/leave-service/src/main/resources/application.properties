spring.application.name=leave-service
server.port=${SERVER_PORT:8082}

spring.datasource.url=${LEAVE_DB_URL:jdbc:postgresql://localhost:5434/leave_db}
spring.datasource.username=${LEAVE_DB_USERNAME:simplehr}
spring.datasource.password=${LEAVE_DB_PASSWORD:simplehr}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

management.endpoints.web.exposure.include=health,info
management.endpoint.health.probes.enabled=true
